# UPD-Volunteer-Management-System

## System Overview

This system consists of two main interfaces:

- Volunteer Registration System (public-facing)
- Admin Dashboard (internal use)

Both systems are connected to a centralized backend API.

## Structure

- /client → Frontend interfaces
- /server → Backend API

## Deployment

Frontend: Vercel  
Backend: Render
